<form>
	<div class="item">
		<div class="content">
	    	<h1 class="herotext"><strong>Solo unos Datos para Contactarnos..</strong></h1>
			<h2 class="ui statistic">Y pronto atenderemos y le responderemos.</h2>
	    </div>
	</div>
<div class="ui clearing divider"></div>
 <div class="ui two column relaxed grid">
  <div class="column">
    <div class="ui fluid form segment">
      <h3 class="ui header">Mensaje a enviar:</h3>
       <div class="field">
        	<h2>Hola les escribe <strong>{{nombre}} {{apellido}}</strong> mi email es <strong>{{email}}</strong> mi contacto es por lo siguiente:</h2>
			<h2><strong>{{mensaje}}</strong></h2>
      </div>
    </div>
     <div class="ui red header">
       
      {{#each message in debuggingErrors}}
         
      {{/each}}
     
    </div>
  </div>
  <div class="column">
    <div class="ui fluid form segment">
      <h3 class="ui header">Llene el Formulario</h3>
      <div class="two fields">
        <div class="field">
          <label>Nombre</label>
          {{input type="text" placeholder="Nombre" class="error" id="thing_name" value=nombre}}
          {{#if errors.nombre.firstObject}}
            <div class="ui red pointing above ui label">{{errors.nombre.firstObject}}</div>
          {{/if}}
        </div>
       <!-- {{#if errors.mensaje.firstObject}}
        	<div class="field error">
         {{/if}}-->
        <div class="field">
          <label>Apellido</label>
          {{input type="text" placeholder="Apellido" value=apellido}}
          {{#if errors.apellido.firstObject}}
            <div class="ui red pointing above ui label">{{errors.apellido.firstObject}}</div>
          {{/if}}
        </div>
      </div>
      
	  	<!-- <div {{bind-attr class=":form-group errors.email.firstObject:has-error"}}>-->
        <label>Email</label>
        <div class="ui left icon input">
        	{{input type="text" placeholder="Agrega tu email" value=email}}
			<i class="mail icon"></i>
	          {{#if errors.email.firstObject}}
	            <div class="ui red pointing above ui label">{{errors.email.firstObject}}</div>
	          {{/if}}
        </div>
        <div class="field">
        <label>Mensaje</label>
        {{textarea valueBinding="mensaje" id="message" name="message"}}
		{{#if errors.mensaje.firstObject}}
	        <div class="ui red pointing above ui label">{{errors.mensaje.firstObject}}</div>
	    {{/if}}
        </div>
      <!--<div class="ui labeled blue toggle submit button" {{action 'Contacto' bind-attr disabled="isInvalid"}}><i class="mail icon"></i>Enviar</div>-->
      <button type="submit" class="btn button" {{bind-attr disabled="isInvalid"}} {{action 'Contacto'}}><i class="mail icon"></i>Enviar</button>
      <div class="ui error message"></div>
    </div>
  </div>
</div>
</form>
<div class="ui clearing divider"></div>

 